"""Evaluation definitions, concurrent pipeline, judging, and baseline servers.

This package contains dataset and prompt definitions, answer extraction,
scoring and metrics, plus OpenAI-compatible servers for C2L, Delta-Mem,
Embedding-RAG, and the recurrent baseline. Run evaluation with
``python -m eval.pipeline``.
"""
